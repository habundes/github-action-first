name: Learning GitHub Actions Name
run-name: I am Learning GitHub Actions Run-name!
on: [push]
jobs:
  hello-world-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Node
        uses: actions/setup-node@v3
        with:
          node-version: 20
      - name: Run script
        run: node index.js